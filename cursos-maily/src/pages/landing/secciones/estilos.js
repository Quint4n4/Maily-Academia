/**
 * Constantes de estilo de la landing.
 *
 * Aparte de `Piezas.jsx` porque un archivo que exporta componentes Y valores
 * rompe el recargado en caliente de Vite: al cambiar el valor, React no sabe
 * si puede conservar el estado y recarga la pagina entera.
 */

/** Margen lateral de todas las secciones: 120 px en escritorio, 24 en movil. */
export const canal = 'px-6 sm:px-10 lg:px-[120px]';
